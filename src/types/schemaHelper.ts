// Define enums for better type safety and validation
export enum OriginType {
  FRONTEND = 'FE',
  BACKEND = 'BE',
  OTHER = 'OTHER',
}

export enum ErrorType {
  CRITICAL = 'critical',
  NORMAL = 'normal',
  WARNING = 'warning',
  INFO = 'info',
}
